# Copy to key.properties (gitignored) next to this file.
# storeFile is relative to android/app/ — use ../upload-keystore.jks when the
# .jks lives in android/ (recommended).
storePassword=CHANGE_ME
keyPassword=CHANGE_ME
keyAlias=sync-upload
storeFile=../upload-keystore.jks
