namespace Iam.Domain.Enums;

/// <summary>
/// Persona mặc định của SYNC agent — ảnh hưởng cách diễn đạt và độ “push”.
/// </summary>
public enum AgentPersona
{
    StrictCoach = 0,
    FriendlyBuddy = 1,
    CalmMentor = 2,
    EnergeticTrainer = 3
}
